---
title: QIRF Quantum-Inspired Non-Orthogonal Function-Space Compression for 3D Gaussian Splatting
title_zh: QIRF：量子启发的非正交函数空间压缩用于3D高斯溅射
authors: "Shizeng Jiang, Hao Zhang, Xuerui Ma, Ying Hu, Tao Zhang"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18067v1"
tags: ["query:pano-dgs"]
score: 8.0
evidence: 3DGS压缩方法，支持高效新视图合成存储
tldr: "3D高斯泼溅(3DGS)用大量各向异性高斯原语实现高质量渲染，但复杂场景需数百万高斯导致高存储与渲染开销。现有压缩方法多关注原语剪枝或量化，忽略了高斯基函数重叠导致的非正交冗余。本文提出量子启发非正交函数空间压缩方法QIRF，通过构造重叠矩阵和辐射响应密度矩阵，利用广义特征分解识别局部子空间并选择代表高斯，同时引入响应模型与细节保护。在13个场景中平均减少71.7%高斯数(约3.54倍压缩)，PSNR提升0.10dB，渲染速度提升34.3%。该工作表明非正交函数空间冗余是显式高斯辐射场中的重要压缩源。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18067v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1507, \"height\": 1240, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18067v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1464, \"height\": 827, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18067v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 809, \"height\": 657, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18067v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1832, \"height\": 324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18067v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1837, \"height\": 185, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18067v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1837, \"height\": 164, \"label\": \"Table\"}]"
motivation: 现有3DGS压缩方法未利用高斯基函数非正交重叠造成的冗余，限制了压缩效率。
method: 将邻近高斯建模为局部非正交基，构造重叠矩阵与辐射响应密度矩阵，通过广义特征分解选择代表子空间，并加入细节保护。
result: "在13个场景中平均减少71.7%高斯数，存储压缩3.54倍，PSNR略升0.10dB，渲染速度提升34.3%。"
conclusion: 非正交函数空间冗余是显式高斯辐射场中的重要压缩源，QIRF有效利用该冗余实现高效压缩。
---

## 摘要
3D高斯溅射（3DGS）通过使用大量各向异性高斯基元表示场景，实现了高质量实时渲染。然而，复杂场景通常需要数百万个高斯基元，导致大量存储和渲染成本。现有压缩方法主要通过基元修剪、属性量化、聚类或神经编码来减少冗余，而由强重叠和非正交高斯基函数引起的冗余在很大程度上仍未探索。我们提出了QIRF，一种用于3D高斯溅射的量子启发非正交函数空间压缩方法。QIRF将相邻高斯基元建模为局部非正交基，并将基元减少表述为子空间感知选择问题。具体而言，构建解析高斯重叠矩阵和辐射响应密度矩阵来表征函数冗余和渲染相关性。然后使用广义特征分解来识别主导局部子空间并选择代表性高斯基元。基于RRDM的响应模型和细节感知保护进一步在激进修剪下保留视觉重要的高频结构。在Mip-NeRF 360、Tanks and Temples和Deep Blending的13个场景上的实验表明，QIRF平均减少了71.7%的高斯计数和原始PLY存储，对应约3.54倍压缩，同时保持与3DGS相当的重建质量，并实现了0.10 dB的边际平均PSNR提升。QIRF还比3DGS平均提高了34.3%的渲染速度。这些结果表明，非正交函数空间冗余是显式高斯辐射场中一种重要但未被充分探索的表征冗余来源。

## Abstract
3D Gaussian Splatting (3DGS) achieves high-quality real-time rendering by representing a scene with a large collection of anisotropic Gaussian primitives. However, complex scenes often require millions of Gaussians, resulting in substantial storage and rendering costs. Existing compression methods mainly reduce redundancy through primitive-wise pruning, attribute quantization, clustering, or neural coding, while redundancy caused by strongly overlapping and non-orthogonal Gaussian basis functions remains largely unexplored. We present QIRF, a quantum-inspired non-orthogonal function-space compression method for 3D Gaussian Splatting. QIRF models neighboring Gaussian primitives as a local non-orthogonal basis and formulates primitive reduction as a subspace-aware selection problem. Specifically, an analytic Gaussian overlap matrix and a radiance-response density matrix are constructed to characterize functional redundancy and rendering relevance. Generalized eigendecomposition is then used to identify the dominant local subspace and select representative Gaussian primitives. An RRDM-based response model and detail-aware safeguarding further preserve visually important high-frequency structures under aggressive pruning. Experiments on 13 scenes from Mip-NeRF 360, Tanks and Temples, and Deep Blending show that QIRF reduces the Gaussian count and raw PLY storage by 71.7 percent on average, corresponding to approximately 3.54 times compression, while maintaining reconstruction quality comparable to 3DGS and achieving a marginal average PSNR improvement of 0.10 dB. QIRF also improves the average rendering speed over 3DGS by 34.3 percent. These results suggest that non-orthogonal function-space redundancy is an important yet underexplored source of representational redundancy in explicit Gaussian radiance fields.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

3D高斯溅射（3DGS）使用大量各向异性高斯基元表示场景，实现了高质量实时渲染。然而复杂场景需要数百万个高斯基元，导致存储和渲染成本巨大。现有压缩方法主要通过基元级修剪、属性量化、聚类或神经编码来减少冗余，但忽略了由于强重叠和非正交高斯基函数引起的函数空间冗余。本文提出QIRF，首次将3DGS压缩建模为局部非正交基约简问题，通过量子化学中非正交基分析的思想，主动识别并去除函数空间冗余，从而在保持重建质量的同时大幅减少基元数量。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
将相邻高斯基元建模为局部非正交基，通过构造重叠矩阵和辐射响应密度矩阵（RRDM），利用广义特征分解识别主导局部子空间，并选择代表性高斯基元，同时用细节感知保护保留精细结构。

### 关键技术细节
- **自适应空间划分**：将场景划分为自适应体素块，根据块稀疏性选择体素大小。
- **局部函数空间建模**：
  - 构造高斯重叠矩阵 \(S_{ij} = \int \phi_i(x)\phi_j(x)dx\)，衡量基元间函数冗余。
  - 构造辐射响应密度矩阵 \(P = \eta \frac{H_r H_r^\top}{\text{tr}(H_r H_r^\top)+\epsilon} + (1-\eta)\frac{H_o H_o^\top}{\text{tr}(H_o H_o^\top)+\epsilon}\)，其中 \(H_r, H_o\) 分别为渲染响应和优化响应堆叠，\(\eta=0.75\)。
- **广义自然场景轨道（GNSO）压缩**：求解广义特征值问题 \(P u_k = \lambda_k \tilde{S} u_k\)，其中 \(\tilde{S} = S + \epsilon I\)。保留能量占比 \(\tau=0.95\) 的模式，计算参与分数 \(q_i = \sum_{k=1}^r \lambda_k |u_{ik}|^2\)，每个块保留 top-\(k_b\) 个基元（\(k_b = \min\{m_b, \max(r_b, \lceil \kappa m_b \rceil)\}\)，\(\kappa=0.22\)）。
- **细节感知保护**：为GNSO非活跃基元计算细节分数 \(d_i = g_i^{\gamma} v_i^{\nu} s_i^{-\rho} \alpha_i^{\delta}\)，保留前5%的高分基元（\(K_{\text{detail}} = \lfloor 0.05 N \rfloor\)）。
- **剪枝后优化**：禁用稠密化，优化损失 \(\mathcal{L} = \mathcal{L}_{\text{3DGS}} + \lambda_{\text{MSE}} \mathcal{L}_{\text{MSE}}\)，\(\lambda_{\text{MSE}}=2\)。

## 3. 实验设计

### 数据集与基准
- **数据集**：Mip-NeRF 360（9个场景）、Tanks and Temples（2个场景）、Deep Blending（2个场景），共13个场景。
- **评估指标**：PSNR、SSIM、LPIPS（重建质量）；#GS（高斯数量）、PLY存储大小、FPS（渲染速度）。
- **对比方法**：原始3DGS、Compact3DGS、MaskGaussian、RadSplat。部分结果引自原论文或官方复现。

## 4. 资源与算力

论文明确说明：所有实验在同一块NVIDIA RTX 4090 GPU上进行，训练30,000次迭代。标准3DGS稠密化到13,000次迭代，RRDM累积，15,000次迭代进行硬剪枝，之后继续优化。训练时间在各数据集上约为12–26分钟（具体见表2）。未提及使用多GPU或其他算力。

## 5. 实验数量与充分性

- **数量**：在13个场景上进行了完整对比实验（表1、表2），包含4种方法+原始3DGS的定量比较；还进行了消融实验（表3、图3），比较了普通特征空间与广义特征空间、有无细节保护的效果。
- **充分性**：覆盖了多个不同类型场景（室内、室外、物体、大规模等），对比了当前主流压缩方法，并且消融实验验证了核心组件的贡献。实验设计较充分，结果客观公平（所有超参数除自适应体素大小外共享）。

## 6. 主要结论与发现

- QIRF平均减少71.7%的高斯数量，原始PLY存储压缩约3.54倍。
- 在保持甚至略微提升重建质量（平均PSNR提升0.10 dB）的同时，渲染速度比3DGS提升34.3%。
- 广义特征分解比普通特征分解更优，细节感知保护能有效改善视觉质量。
- 非正交函数空间冗余是显式高斯辐射场中重要但未被充分探索的冗余来源。

## 7. 优点

- **新颖视角**：首次将3DGS压缩问题形式化为非正交基约简，借鉴量子化学广义特征分解，理论新颖。
- **高压缩比**：在同等质量下达到最高压缩率（所有数据集上高斯数最少）。
- **质量保持**：不仅未显著下降，甚至略有提升（PSNR+0.10 dB）。
- **细节保护机制**：通过细节分数保留细小结构，避免过度剪枝导致视觉退化。
- **实验全面**：在三个主流数据集上对比多种方法，并做消融分析。

## 8. 不足与局限

- **未结合属性压缩**：当前框架主要针对基元数量级压缩，未集成属性量化和熵编码，因此存储结果仅为原始PLY压缩，可进一步优化。
- **自适应划分不够灵活**：体素大小仅从小候选集中选择，可能不适用于极端复杂场景。
- **渲染速度不是最优**：虽然比3DGS快34.3%，但比MaskGaussian等专门优化渲染的方法慢，表明系统级优化（如稀疏光栅化）未充分实施。
- **训练时间略长**：在Deep Blending上训练时间（20m56s）比MaskGaussian（16m37s）长，主要由于局部矩阵构造和广义特征分解的计算开销。
- **未考虑多视角一致性**：细节保护分数基于单次训练过程中的梯度/可见性，可能对视角依赖性未充分建模。
- **超参数固定**：除体素大小外所有超参数跨场景固定，在极端分布场景下可能需要调优。

（完）
