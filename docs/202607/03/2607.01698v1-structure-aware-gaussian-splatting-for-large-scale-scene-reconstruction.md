---
title: Structure-Aware Gaussian Splatting for Large-Scale Scene Reconstruction
title_zh: 面向大规模场景重建的结构感知高斯溅射
authors: "Weiyi Xue, Fan Lu, Chi Zhang, Tianhang Wang, Sanqing Qu, Zehan Zheng, Boyuan Zheng, Junqiao Zhao, Guang Chen"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01698v1"
tags: ["query:pano-dgs"]
score: 8.0
evidence: 结构感知3DGS用于大规模场景重建，提升效率和质量
tldr: 大规模场景重建中，稀疏观测区域导致初始点低频与高频图像监督不匹配，引发高斯点冗余和效率下降。本文提出结构感知调度器SIG，通过感知场景频率收敛，动态调节图像分辨率与高斯密集化过程，同步图像监督与高斯频率。引入球面约束高斯，利用点云空间先验控制优化。该方法在多个大规模场景上渲染质量与效率均达最优，实现了频率一致、几何感知的无浮动点训练。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01698v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1560, \"height\": 478, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01698v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1401, \"height\": 451, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01698v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1262, \"height\": 288, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01698v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1364, \"height\": 716, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01698v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1451, \"height\": 464, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01698v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1558, \"height\": 349, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01698v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 776, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01698v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 780, \"height\": 246, \"label\": \"Table\"}]"
motivation: 低频率稀疏初始点与高频图像监督不匹配，导致高斯密集化失控和冗余，降低效率与质量。
method: 提出SIG调度器，根据频率收敛调节图像分辨率与密集化；引入球面约束高斯利用点云先验。
result: 在大规模场景中渲染质量和效率均大幅超越现有方法，减少冗余高斯。
conclusion: 实现频率一致、几何感知的无浮动点训练，适用于大规模场景重建。
---

## 摘要
3D高斯溅射在新视角合成中展现出显著潜力。与小规模场景不同，大规模场景不可避免地包含稀疏观测区域，其初始点极为稀疏。在这种情况下，用高频图像监督从低频稀疏点初始化的高斯函数，常常会导致不受控制的致密化和冗余基元，降低效率和质量。直观上，可以通过调度策略缓解这一问题，调度策略可分为两种范式：通过致密化调制目标信号频率，以及通过图像分辨率调制采样频率。然而，先前的调度策略主要是硬编码的，无法感知场景频率的收敛行为。为解决此问题，我们从信号结构恢复的角度重新构建场景重建问题，并提出了SIG——一种将图像监督与高斯频率同步的新型调度器。具体而言，我们推导了3D表示的平均采样频率和带宽，然后根据场景频率收敛情况调节训练图像分辨率和高斯致密化过程。此外，我们引入了球面约束高斯函数，利用初始化点云的空间先验来控制高斯优化。我们的框架实现了频率一致、几何感知且无漂浮物的训练，在大规模场景中，效率和渲染质量均大幅领先于现有技术，达到了最先进的性能。代码开源地址：https://github.com/weiyixue999/Signal_Structure_Aware_Gaussian

## Abstract
3D Gaussian Splatting has demonstrated remarkable potential in novel view synthesis. In contrast to small-scale scenes, large-scale scenes inevitably contain sparsely observed regions with excessively sparse initial points. In this case, supervising Gaussians initialized from low-frequency sparse points with high-frequency images often induces uncontrolled densification and redundant primitives, degrading both efficiency and quality. Intuitively, this issue can be mitigated with scheduling strategies, which can be categorized into two paradigms: modulating target signal frequency via densification and modulating sampling frequency via image resolution. However, previous scheduling strategies are primarily hardcoded, failing to perceive the convergence behavior of scene frequency. To address this, we reframe the scene reconstruction problem from the perspective of signal structure recovery and propose SIG, a novel scheduler that synchronizes image supervision with Gaussian frequencies. Specifically, we derive the average sampling frequency and bandwidth of 3D representations, and then regulate the training image resolution and the Gaussian densification process based on scene frequency convergence. Furthermore, we introduce Sphere-Constrained Gaussians, which leverage the spatial prior of initialized point clouds to control Gaussian optimization. Our framework enables frequency-consistent, geometry-aware, and floater-free training, achieving state-of-the-art performance by a substantial margin in both efficiency and rendering quality in large-scale scenes. The code is available at: https://github.com/weiyixue999/Signal_Structure_Aware_Gaussian

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：大规模场景重建中，稀疏观测区域导致初始点云极度稀疏，低频初始点与高频图像监督不匹配，引发高斯点失控密集化、产生大量冗余基元和漂浮物（floater），降低效率与重建质量。
- **背景**：现有3DGS方法在小规模场景表现优异，但在大规模场景中面临效率瓶颈；硬编码的调度策略（如固定分辨率或固定密集化时机）无法感知场景频率的收敛状态，导致过早引入高分辨率或延迟必要细化。
- **整体含义**：本文从信号结构恢复视角重新定义场景重建，提出一种能感知场景频率收敛的自适应调度框架，实现频率一致、几何感知的训练，显著提升大规模场景的渲染质量和训练效率。

### 2. 论文提出的方法论

**核心思想**：将场景重建视为从离散采样图像中恢复连续3D信号的过程。通过数学定义3D高斯表示的平均频率和场景信号带宽，设计调度器SIG，根据场景频率收敛动态调整图像分辨率和高斯密集化过程；同时引入球面约束高斯，利用点云空间先验减少冗余和漂浮物。

**关键技术细节**：

- **平均采样频率**：基于图像焦距 \(f\) 与深度 \(d\) 定义局部采样频率 \(v \propto f/d\)，并加权平均得到全局平均采样频率（公式1）。
- **场景信号带宽**：通过3D高斯不透明度场的傅里叶分析，利用高斯函数的半功率带宽 \(\omega_{\text{3dB}} \propto 1/\sigma\)，近似计算平均场景带宽（公式5）。实验验证该定义与采样频率正相关，且随训练迭代稳定增长。
- **频率对齐分辨率调度器**：计算每个迭代的 \(\bar{\omega}\) 及其梯度 \(d\bar{\omega}/d\text{iter}\)，当梯度小于阈值 \(k \cdot \text{mean}(1/d)\) 时（\(k=5\times10^{-5}\)），判断当前分辨率下的带宽收敛，自动提升图像分辨率（公式6）。
- **密集化调度器**：每次分辨率更新后，执行 \(m\) 轮密集化操作，避免早期过度密集化。
- **球面约束高斯**：为每个高斯分配锚点（anchor）和最大偏移量，初始锚点为COLMAP点，偏移量基于K近邻平均距离设定。当高斯偏移超过 \(l \times \text{max\_offset}\)（\(l=15\)）时剪枝。新生成的高斯继承规则：复制时不继承锚点，分裂时继承锚点并缩小最大偏移量至0.7倍。
- **密集化正则化**：利用渲染深度图投影到相邻帧计算光度一致性损失 \(L_{\text{cons}}\)（公式7），只用于密集化阶段。

### 3. 实验设计

- **数据集/场景**：
  - 真实世界：Mill19（building, rubble, residence, sci-art）
  - 合成：MatrixCity（Aerial）
- **基准对比方法**：
  - NeRF类：Mega-NeRF、SwitchNeRF
  - GS类：DashGS、VastGS、CityGS、DOGS、BlockGS
- **评估指标**：PSNR、SSIM、LPIPS（颜色校正后）
- **消融实验**：在rubble场景上分别移除频率对齐分辨率调度器（w/o FARS）、密集化调度器（w/o DS）、球面约束高斯（w/o SCG）、密集化正则化（w/o DR），以及使用所有模块（w/ all）。
- **集成实验**：将本文调度策略嵌入BlockGS和CityGS，观察质量与效率提升。

### 4. 资源与算力

- **GPU型号与数量**：文中明确“All methods are trained and evaluated on NVIDIA RTX 4090 GPUs”，未说明具体数量，推测使用单卡训练每个block（因采用分块策略）。
- **训练时长**：粗训练和精训练各30,000次迭代。报告了平均每块优化时间：CityGS+*为71 min/block，BlockGS+*为130 min/block（原分别为98和201 min/block）。
- **结论**：算力信息部分明确（RTX 4090），但未给出总GPU数量和并行细节。

### 5. 实验数量与充分性

- **主实验**：在5个场景（3真实+1合成？实际为4个Mill19子场景+MatrixCity）上对比8种方法，给出定量结果（Table 1）及定性图像对比（Figure 4）。
- **消融实验**：在rubble场景上进行4组单模块消融 + 全套实验，报告PSNR/SSIM/LPIPS及高斯点数（Table 2）。
- **集成实验**：在BlockGS和CityGS上各进行一组增广对比（Table 3）。
- **充分性**：
  - 数据集覆盖真实与合成，规模多样，但缺少车载街景或更大规模数据集（如Waymo、KITTI-360）验证泛化性。
  - 消融实验设计清晰，验证了每个模块的贡献。
  - 与最新方法（2024-2025年）对比，包括硬编码调度方法DashGS。
  - 但未与近期基于神经隐式方法的GS变体（如Octree-GS、MomentumGS）直接对比（虽在related work提及）。
- **公平性**：所有方法均在相同硬件上训练，采用标准颜色校正，且调整了BlockGS的batch size以公平对比（但导致其时间增加）。

### 6. 论文的主要结论与发现

- 提出的平均高斯频率定义与采样频率正相关且随训练收敛，可用于指导自适应调度。
- 频率对齐的调度策略相比硬编码调度，能有效避免早期过度密集化，提升重建质量（+0.9 dB PSNR in rubble）和训练速度（1.5× per block）。
- 球面约束高斯结合密集化正则化，显著减少冗余点和漂浮物，保持几何结构。
- 本文方法可即插即用增强现有基线（BlockGS、CityGS），同时提升质量与效率。

### 7. 优点

- **理论创新**：首次从信号处理角度为3DGS定义平均采样频率和场景带宽，提供了自适应调度的理论基础。
- **工程实用**：SIG调度器无需手工调参，完全基于频率收敛自动执行，可嵌入多种现有方法。
- **高效节资源**：通过低分辨率早期优化结构，高分辨率后期细化纹理，减少冗余计算；球面约束降低无效高斯数量。
- **泛化性强**：在多个大型场景上取得一致先进结果，消融实验充分验证各模块有效性。

### 8. 不足与局限

- **深度估计不完美**：密集化正则化依赖渲染深度，而GS深度准确性有限，投影误差会引入噪声，作者指出在全优化中持续使用此损失并无提升。
- **缺少LoD原生支持**：大规模场景通常需要层次细节渲染，本文仅依赖现有策略（如CityGS），粗到细训练虽可自然产生层次，但未明确实现LoD结构。
- **评估范围局限**：未在自动驾驶或户外穿行等大规模动态场景上测试；仅对比了有限几种基线，未与近期更多GS变体（如Octree-GS、MomentumGS）比较。
- **参数选择依赖先验**：球面约束的尺度因子 \(l=15\)、近邻数 \(K=15\) 基于实验设定，理论上对场景密度敏感，可能需手工调整。
- **实验细节缺失**：未报告总GPU数量和并行训练配置，复现难度增加；未说明分辨率调度中 \(m\) 的具体值。

（完）
