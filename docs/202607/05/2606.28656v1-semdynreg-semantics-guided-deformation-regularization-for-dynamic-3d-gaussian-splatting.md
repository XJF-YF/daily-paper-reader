---
title: "SemDynReg: Semantics-Guided Deformation Regularization for Dynamic 3D Gaussian Splatting"
title_zh: SemDynReg：语义引导的动态3D高斯溅射变形正则化
authors: "Ruitao Chen, Mozhang Guo, Jinge Li"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28656v1"
tags: ["query:pano-dgs"]
score: 9.0
evidence: 使用语义引导的变形正则化进行动态新颖视图合成
tldr: 动态3DGS中缺乏对象级建模导致高斯变形不一致和对象间耦合。本文提出SemDynReg框架，利用SAM提取语义特征构建对象ID图，对每个对象的前k个高斯施加变形参数一致性正则化。该方法在多个动态场景中提升了渲染质量，PSNR和SSIM提高，LPIPS降低。首次将语义引导的变形正则化引入动态3DGS，实现显式对象级约束。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28656v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1862, \"height\": 1093, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28656v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1881, \"height\": 1136, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28656v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1882, \"height\": 1131, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28656v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 932, \"height\": 401, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28656v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 932, \"height\": 397, \"label\": \"Table\"}]"
motivation: 现有动态3DGS方法忽略对象级建模，导致目标内高斯变形不一致且不同对象间产生不期望的耦合。
method: 利用SAM提取语义特征并构建对象ID图，对每个对象的top-k高斯施加位置、尺度、旋转的一致性正则化。
result: 相比基线，PSNR和SSIM提升，LPIPS降低，对象级变形一致性显著增强。
conclusion: 语义引导的变形正则化有效提升动态3DGS渲染质量和对象级一致性。
---

## 摘要
可变形3D高斯溅射（3DGS）已成为在广泛的3D应用中渲染动态场景的高效方法。然而，现有的基于变形场的方法大多缺乏显式的对象级建模，通常导致单个对象内的高斯变形不一致以及不同对象之间的不必要耦合。为了解决这一限制，我们引入了一个语义引导的框架，在对象级别实施动态正则化，旨在实现空间一致的对象级变形。具体而言，我们首先使用分割任意模型（SAM）提取分割掩码，并从输入图像中推导语义特征。然后通过特征相关性匹配与预定义对象字典构建对象ID图。在此对象ID图的引导下，我们为每个对象识别逐像素的前k个贡献高斯，并对其变形参数（包括位置、尺度和旋转）施加一致性正则化。与先前在没有显式对象级约束的情况下学习变形场的方法不同，我们的方法结合语义线索来指导对象级别的变形行为。实验结果表明，我们的语义感知正则化提高了对象级变形一致性，并在渲染质量上优于基线方法，在动态3DGS渲染中实现了更高的PSNR和SSIM以及更低的LPIPS。我们的项目页面可在https://dyn-reg-3dgs.github.io/上访问。

## Abstract
Deformable 3D Gaussian Splatting (3DGS) has emerged as an efficient approach for rendering dynamic scenes in a wide range of 3D applications. However, existing deformation field-based approaches largely lack explicit object-level modeling, often resulting in inconsistent Gaussian deformations within individual objects and unwanted coupling between different objects. To address this limitation, we introduce a semantics-guided framework that enforces dynamic regularization at the object level, aiming to achieve spatially consistent object-wise deformation. Specifically, we first extract segmentation masks using the Segment Anything Model (SAM) and derive semantic features from input images. An object-ID map is then constructed via feature relevance matching with a predefined object dictionary. Guided by this object-ID map, we identify the pixel-wise top-k contributing Gaussians for each object and impose consistency regularization on their deformation parameters, including position, scale, and rotation. Unlike prior methods that learn deformation fields without explicit object-level constraints, our approach incorporates semantic cues to guide deformation behavior at the object level. Experimental results demonstrate that our semantics-aware regularization improves object-level deformation consistency and outperforms baseline methods in rendering quality, achieving higher PSNR and SSIM and lower LPIPS in dynamic 3DGS rendering. Our project page is available at https://dyn-reg-3dgs.github.io/.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究问题**：现有可变形3D高斯溅射（Dynamic 3DGS）方法在渲染动态场景时，缺乏显式的对象级建模。这导致同一对象内的高斯变形不一致，以及不同对象之间产生不希望的耦合，从而出现视觉伪影（如车辆结构扭曲、运动模糊），降低了渲染的逼真度和物理合理性。
- **背景**：动态3DGS通过连续变形场驱动高斯运动，广泛应用于自动驾驶仿真、虚拟现实等领域。但由于变形场的学习是对象无关的，每个高斯独立变形，无法保证属于同一物体的高斯具有一致的变形行为。已有工作主要依赖时间、几何或运动先验，但很少利用语义信息对变形参数施加结构化约束。
- **研究意义**：引入语义引导的对象级变形正则化，能够使同一物体的高斯变形更协调、物体边界更清晰，从而提升动态场景渲染质量和运动表示的真实性。

## 2. 论文提出的方法论

### 核心思想
利用从输入图像中提取的语义线索（SAM + CLIP）构建对象ID图，识别属于同一物体的高斯集合，对其变形参数（位置、尺度、旋转）施加一致性的正则化约束，迫使物体内的高斯变形保持相似。

### 关键技术细节与流程（文字说明）
1. **语义特征提取**：对每帧输入图像，使用SAM获得非重叠的分割掩码；对每个掩码区域，用CLIP图像编码器提取512维语义特征，形成稠密特征图。
2. **对象ID图构建**：预定义对象字典（如"white SUV"），用CLIP文本编码器得到文本嵌入。计算每个像素的语义特征与各对象文本嵌入的归一化相关性分数，设定阈值τ，将像素分配给得分最高的对象或背景，得到对象ID图。
3. **Top-k高斯选择与对象分组**：利用3DGS渲染中的alpha混合权重，对每个像素选取贡献权重最大的k个高斯索引；再根据对象ID图，将所有属于同一对象的像素对应的top-k高斯索引聚合，形成每个对象的高斯集合。
4. **变形一致性正则化**：对每个对象的高斯集合，计算变形参数（平移δx、旋转δr、缩放δs）的均值，然后用MSE惩罚各高斯参数与均值的偏差。可跳过静态对象（平均平移小于阈值τd）。最终正则化损失与RGB重建损失联合优化，公式：\( L = L_{rgb} + \lambda L_{reg} \)。正则化在训练后段（10k步后）激活。

## 3. 实验设计

- **数据集**：作者构建了一个私有数据集（因为公开数据集缺少所需的语义结构）。场景为包含三辆微型玩具车的平面动态场景，其中一辆运动、其余静止，用单目运动相机多帧拍摄，分辨率1440×1080。共两个场景（场景1白色SUV运动，场景2黄色轿车运动）。
- **Benchmark**：无公开基准，作者以不施加语义正则化的同一变形3DGS管道作为基线（"Baseline (no Reg.)"）。
- **对比方法**：仅与自身基线对比（无正则化 vs. 有正则化），未与其他SOTA方法（如4DGS、Deformable 3DGS等）直接比较，因为重点在于验证正则化效果。

## 4. 资源与算力

- 文中明确提到：训练一个场景约25分钟，使用单块NVIDIA RTX 3090 GPU，显存约4GB。
- 未提及总实验耗时、GPU数量、并行训练等信息，但可知算力需求较低。

## 5. 实验数量与充分性

- **实验组数**：仅在两个私有场景上进行了定性+定量对比（每个场景含6个时间步）。每个时间步计算了SSIM、PSNR、LPIPS共3个指标。表中列出了每个时间步和平均值。未做消融实验（如不同k值、不同λ、是否跳过静态对象等），也未在公开数据集上测试。
- **充分性与公平性**：实验数量较少，场景简单（仅有单一运动物体），未涵盖非刚体、多物体运动、遮挡等复杂情况。基线设置合理（同一管道去除正则化），但缺乏与多种现有方法的横向对比，不足以全面证明方法的通用性。实验客观性尚可，但充分的消融和更大规模验证缺失。

## 6. 论文的主要结论与发现

- 所提出的语义引导变形正则化显著提升了运动物体的渲染质量：在两个场景中，相比基线，PSNR平均提升约3.2~3.8 dB，SSIM平均提升约0.04~0.09，LPIPS平均降低约0.03~0.05。
- 定性结果展示：物体边界更清晰、纹理细节更好（如车身上的条纹）、运动模糊减少，物体外观随时间更稳定。
- 静态物体质量基本不变，说明正则化仅针对动态对象，副作用小。
- 证明了语义信息可有效引入动态3DGS以提升对象级变形一致性。

## 7. 优点

- **创新性**：首次提出将语义引导的变形正则化引入动态3DGS框架，实现显式对象级约束，弥补了现有方法对象无关的缺陷。
- **方法简洁有效**：基于现成的SAM和CLIP，无需额外标注，且正则化项可直接插入已有管道，计算成本低。
- **直观的合理性**：利用top-k贡献高斯而非单一高斯，更鲁棒地覆盖对象。
- **定性定量结合**：提供了可视化对比和数值指标，验证效果明显。

## 8. 不足与局限

- **实验覆盖严重不足**：仅在两个自制简单场景上测试，未使用任何公开动态场景数据集（如Waymo、KITTI、NeRF-DS等），难以保证方法在真实复杂场景（多物体、非刚体、光照变化）中的泛化性。
- **缺乏消融实验**：未分析超参数（k、λ、τ、激活时机等）的影响，也未比较不同分组策略或特征提取方式。
- **未与现有SOTA方法对比**：例如4D-GS、Deformable 3DGS、SC-GS、AD-GS等，无法体现相对优越性。
- **假设刚性运动**：正则化假设对象内部变形一致，对于非刚体（如行人、动物）可能过于严格。虽然文中提到可扩展至部件级，但未实现验证。
- **依赖预定义对象字典**：需手动指定对象类别，限制了无先验场景的适用性。
- **仅考虑单目动态场景**：未测试多视角或叠加闭环场景。
- **潜在偏差风险**：数据集由作者自己采集，可能存在选择偏向；仅报道平均表现，未给出方差或统计显著性检验。

（完）
