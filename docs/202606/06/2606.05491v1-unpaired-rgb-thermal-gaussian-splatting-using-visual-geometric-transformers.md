---
title: Unpaired RGB-Thermal Gaussian-Splatting Using Visual Geometric Transformers
title_zh: 使用视觉几何变换器的未配对RGB-热高斯喷溅
authors: "Jean Cordonnier, Chenghao Xu, Olga Fink, Malcolm Mielle"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.05491v1"
tags: ["query:pano-dgs"]
score: 8.0
evidence: 使用3D高斯泼溅的无配对多模态RGB-热成像新颖视图合成
tldr: 多模态新视角合成中，RGB与热像通常需要精确配对标定，限制了实际应用。本文提出未配对的RGB-热高斯散射框架，利用VGGT独立估计每模态相机位姿，通过Procrustes算法和跨模态特征匹配实现联合对齐，无需配对数据。进一步提出多模态3D高斯散射方法直接从未配对图像学习。实验表明，该方法在热视图合成上达到竞争性能，同时保持RGB质量。此外，引入基准框架严格评估模态内合成与跨模态一致性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05491v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1605, \"height\": 467, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05491v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1799, \"height\": 389, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05491v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1445, \"height\": 1131, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05491v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 901, \"height\": 430, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05491v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1351, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05491v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 562, \"height\": 230, \"label\": \"Table\"}]"
motivation: 现有RGB-热多模态新视角合成依赖精确标定或立体设置，难以大规模部署，需无配对方法。
method: 使用VGGT独立估计位姿，Procrustes对齐多模态特征，结合多模态3D高斯散射从未配对RGB和热图学习。
result: 在热视图合成上达到竞争性能，RGB保真度良好，且跨模态一致性优于单独重建。
conclusion: 提出无配对的RGB-热高斯基元框架，降低标定需求，并提供多模态一致性评估基准。
---

## 摘要
结合RGB和热成像的多模态新视角合成能够利用视觉和热信息实现精确的3D场景重建。然而，现有方法通常依赖精确校准的RGB-热图像对或立体设置，限制了可扩展性和实际部署。为解决这一问题，我们提出一种未配对RGB-热NVS框架，利用VGGT（一种3D前馈变换器架构）独立估计每种模态的相机姿态。然后使用Procrustes算法结合跨模态特征匹配器对齐姿态集，无需配对校准即可实现联合配准。基于此对齐，我们进一步提出一种多模态3D高斯喷溅方法，直接从未配对的RGB和热图像中学习。在多种场景上的实验表明，我们的方法在热视图合成中取得了具有竞争力的性能，同时保持了RGB保真度。此外，我们展示了现有重建方法可能产生缺乏跨模态一致性的模态特定重建。因此，我们引入一个基准测试框架来严格评估各模态图像合成和重建场景的多模态一致性。

## Abstract
Multi-modal novel view synthesis (NVS) combining RGB and thermal imagery enables precise 3D scene reconstruction with visual and thermal information. However, existing methods typically rely on precisely calibrated RGB-thermal image pairs or stereo setups, limiting scalability and practical deployment. To address this, we introduce a framework for unpaired RGB-thermal NVS that leverages VGGT, a 3D feed-forward transformer architecture, to independently estimate camera poses for each modality. The pose sets are then aligned using the Procrustes algorithm with a cross-modal feature matcher, enabling joint registration without paired calibration. Building on this alignment, we further propose a multi-modal 3D Gaussian Splatting approach that learns directly from unpaired RGB and thermal images. Experiments on diverse scenes demonstrate that our method achieves competitive performance in thermal view synthesis while maintaining RGB fidelity. Moreover, we show that existing reconstruction approaches can produce modality-specific reconstructions that lack cross-modal consistency. We thus introduce a benchmarking framework to rigorously evaluate both per-modality image synthesis and the multi-modal coherence of reconstructed scenes.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：多模态（RGB-热成像）新视角合成（NVS）能够结合视觉和热信息实现精确3D重建，但现有方法依赖精确校准的RGB-热图像对或立体设置，成本高且难以扩展。热图像纹理弱、存在鬼影效应，导致传统SfM方法难以估计其位姿；而立体跨模态标定复杂。因此，需要一种无需配对数据、能独立采集RGB和热图像后进行联合重建的框架。
- **核心问题**：如何从未配对的RGB和热图像集中，估计一致的相机位姿并训练多模态3D高斯抛雪模型，实现跨模态的重建一致性。
- **整体含义**：首次提出完全无需配对RGB-热图像的多模态NVS方法，通过VGGT独立估计位姿、XoFTR特征匹配和Procrustes对齐，再结合多模态3DGS训练，降低了部署门槛，并引入跨模态一致性评估基准。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：两个独立步骤：先分别用VGGT从RGB和热图像集估计相机参数（位姿、内参、深度图等），再用跨模态特征匹配器（XoFTR）建立两模态间的2D对应，进而投影到各自点云获得3D对应点，通过Procrustes算法（相似变换）对齐两套位姿，最后用对齐后的位姿训练一个联合的3DGS模型（每个高斯同时包含RGB颜色和热温度）。
- **关键技术细节**：
  - **VGGT初始化**：直接将三通道热图像（单通道复制到三个通道）输入VGGT，无需微调即可估计热图像的3D属性，证明了VGGT的泛化能力。
  - **跨模态对齐**：
    - 使用XoFTR匹配RGB-热图像对，用MAGSAC滤除外点，保留超过阈值t的对。
    - 用VGGT输出的深度图和位姿构建各模态点云，丢弃低置信度点（低于50%分位数）。
    - 对筛选后的3D对应点集，使用Procrustes算法求解相似变换 (s, R, t)，优化目标：min Σ ||p_RGB - (s R p_T + t)||²。
    - 迭代过程：每次丢弃残差最大的10%点，重新计算变换。
    - 将求得的相似变换应用于热图像的位姿，使其与RGB位姿在同一坐标系。
  - **3D热高斯抛雪训练**：
    - 每个高斯参数包含位置、协方差、不透明度、RGB颜色（球谐编码）、热温度（球谐编码）。
    - 损失函数：L_total = L_SSIM(T_pred, T_gt) + L_SSIM(RGB_pred, RGB_gt)，其中L_SSIM结合MAE和1-SSIM。
    - 无需配对约束：每个epoch随机打乱RGB和热图像，渲染任意一对计算损失。
- **算法流程**：输入两套图像 → VGGT独立估计位姿、深度、点云 → XoFTR特征匹配 + 置信度过滤 → Procrustes对齐 → 将热位姿变换到RGB坐标系 → 3DGS联合优化。

## 3. 实验设计：数据集/场景、基准方法、对比方法
- **数据集/场景**：选取9个不同场景，来自ThermalGaussian和ThermoScenes数据集（均为配准的RGB-热图像对）。包括：Building A (Spring), Kettle, Truck, INR Building等。作者排除了XoFTR无法建立可靠匹配的场景，以确保公平评估。
- **基准方法**：ThermalGaussian (T-GS)，这是理想条件下的方法（RGB和热图像配对，位姿来自COLMAP）。对比设置：
  - T-GS RGB：用RGB位姿同时训练RGB和热。
  - T-GS T：用热位姿同时训练两种模态。
  - Ours RGB：本文方法，测试时用RGB位姿渲染RGB和热。
  - Ours T：本文方法，测试时用热位姿渲染RGB和热。
- **评价指标**：
  - 图像质量：PSNR、SSIM、LPIPS（仅RGB）。
  - 温度精度：MAE（预测温度与真值）。
  - 位姿精度：相对位姿误差（RPE）和绝对轨迹误差（ATE），所有场景缩放到1m球体内比较。
  - 分模态内（Intra-modal）和跨模态（Inter-modal）指标：模态内用各自模态位姿评估；跨模态用另一种模态的位姿评估。

## 4. 资源与算力
- 论文中**未明确说明**使用的GPU型号、数量、训练时长等具体算力资源。只提到使用默认参数（如nerfacto默认参数）以及一些超参数（如最小XoFTR匹配数）。因此无法总结算力细节。

## 5. 实验数量与充分性
- **实验数量**：在主实验（表I）中，给出9个场景的平均结果。另外有单独的表II提供部分场景的可视化对比；表III给出位姿质量（RPE/ATE）的均值、中位数和RMSE；还有手动对齐的定性对比（图3）。总共涉及三类实验：NVS指标对比、位姿精度、跨模态一致性。
- **充分性**：
  - **优点**：设计了intra-modal和inter-modal两组指标，揭示了仅看模态内指标可能掩饰跨模态不一致的问题，并用图3可视化证明。对比了理想基线T-GS，相对公平。
  - **不足**：
    - 仅针对9个场景，且排除了XoFTR匹配失败的情况，可能高估了方法在复杂场景的鲁棒性。
    - 没有进行消融实验（如去掉Procrustes、仅用VGGT、不同匹配方法等）。
    - 没有与更简单的方法对比（如直接ICP对齐VGGT位姿，或基于COLMAP+RANSAC）。
    - 没有分析不同图像数量或视角分布对结果的影响。

## 6. 论文的主要结论与发现
- **结论1**：VGGT可以直接用于热图像（无需微调）进行3D属性估计，展现了强大的跨模态泛化能力。
- **结论2**：通过本文的Procrustes对齐和跨模态特征匹配，可以从完全未配对的RGB和热图像集中实现多模态NVS，热视图合成质量接近理想配对基线（特别是在使用热位姿时，Ours T甚至优于T-GS T）。
- **结论3**：位姿对齐至关重要——若仅依赖手动/ICP对齐（忽略尺度），模型会学习两个独立的单模态表示，导致跨模态渲染严重错位（如图3），而intra-modal指标却仍能很高。因此必须引入cross-modal指标评价。
- **结论4**：与T-GS相比，本文方法在RGB位姿下的性能略低（因RGB位姿更精确），但在热位姿下表现更好，证明混合未配对数据训练对热模态重建有益。

## 7. 优点
- **方法上的亮点**：
  - 首次实现完全无配对的多模态RGB-热NVS，无需立体标定或配准。
  - 利用VGGT的零样本能力处理热图像，免去热领域微调。
  - 结合XoFTR和Procrustes解决不同尺度下的位姿对齐，且采用迭代剔除离群点策略。
  - 提出在3DGS中联合优化RGB和热属性，并采用随机配对训练，适应未配对输入。
- **实验设计亮点**：
  - 明确提出intra-modal和inter-modal指标，暴露已有方法潜在问题。
  - 通过手动对齐实验（图3）直观证明跨模态对齐不足导致的灾难性后果。
  - 进行了位姿精度评估（ATE/RPE）。

## 8. 不足与局限
- **实验覆盖局限**：
  - 仅使用9个场景，且排除了XoFTR匹配失败的情况，无法评估方法在低纹理或极端热变化场景的鲁棒性。
  - 没有进行消融实验，无法量化各模块（VGGT、Procrustes、特征匹配、随机配对训练）的贡献。
  - 没有与其他跨模态方法（如基于监督学习的热-位姿回归）对比。
- **偏差风险**：
  - 数据集来源单一（ThermalGaussian和ThermoScenes），可能偏向特定采集条件。
  - 测试时仍使用配对的RGB-热图像（仅用RGB位姿），本质上引入了部分监督信息，不是完全“未配对”测试。
- **应用限制**：
  - 依赖VGGT和XoFTR的鲁棒性，若两模态拍摄视角/光照差异过大，特征匹配可能失败。
  - 未考虑动态场景或温度随时间变化的场景（如热辐射变化）。
  - 未评估在真实未配准数据（如不同时间、不同机器人采集）上的表现。
- **其他**：缺少对温度值绝对精度的深入分析（MAE约2.5°C~4.9°C，实际应用可接受度未知）；缺少计算效率对比（训练时间、渲染速度）。

（完）
