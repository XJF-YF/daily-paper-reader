---
title: "MoVerse: Real-Time Video World Modeling with Panoramic Gaussian Scaffold"
title_zh: MoVerse：基于全景高斯支架的实时视频世界建模
authors: "Yang Zhou, Ziheng Wang, Yuqin Lu, Haofeng Liu, Jun Liang, Shengfeng He, Jing Li"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13376v1"
tags: ["query:pano-dgs"]
score: 9.0
evidence: 使用高斯散射重建全景360度场景
tldr: 从单张窄视场图像实时创建可交互漫游的360度视频世界模型。通过拓扑感知扩散补全全景图，再用全景几何感知残差预测提升为持久3D高斯脚手架，最后用扩散教师蒸馏的自回归学生模型实现低延迟高清视频渲染。在RTX 4090上达到8 FPS。该方法结合显式3D表示的稳定性和生成模型的感知质量，实现单图到交互视频的实用路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13376v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1703, \"height\": 712, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1622, \"height\": 696, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 822, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1718, \"height\": 805, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1716, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1705, \"height\": 898, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1719, \"height\": 1320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1532, \"height\": 2155, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1722, \"height\": 1318, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1546, \"height\": 1065, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1547, \"height\": 1031, \"label\": \"Figure\"}]"
motivation: 单图像交互式场景漫游需要从有限视角重建完整、一致、可控制的3D世界，并生成高质量视频。
method: 先拓扑感知扩散生成全景图，再全景残差预测构建3D高斯脚手架作为持久空间记忆，最后用双向扩散教师蒸馏为自回归学生，实现可控低延迟渲染。
result: 支持实时以8 FPS在RTX 4090上交互漫游，输出高保真、时空一致的视频。
conclusion: 分离世界构建与渲染，结合显式3D表示与生成模型，有效实现单图到交互视频的实时生成。
---

## 摘要
我们提出MoVerse，一种实时视频世界模型，能够从单张窄视场图像创建可交互导航的场景。这一设定具有挑战性，因为输入仅观察环境的一小部分，而交互式漫游需要完整的周围世界、持久几何、可控相机运动以及时间一致的高保真观测。MoVerse通过将世界构建与观察渲染分离来解决此问题。它首先利用拓扑感知扩散将输入扩展为重力对齐的360°全景图，在3D推理之前闭合缺失的视场。然后，通过全景几何感知残差预测将全景图提升为持久3D高斯支架，生成密集且可直接渲染的空间记忆。最后，高斯条件视频渲染器将沿用户指定相机轨迹的支架渲染结果转换为逼真的视频。为了使该渲染器适用于交互，我们训练一个双向扩散教师模型用于高质量条件渲染，并将其蒸馏为因果自回归学生模型以实现有限延迟流式传输。这种设计将显式3D表示的可控性和长程一致性与生成视频模型的感知质量相结合。MoVerse在单个NVIDIA RTX 4090 GPU上支持8 FPS的实时场景漫游，展示了具有交互视频输出的单图像世界创建的实用路径。

## Abstract
We present MoVerse, a real-time video world model that creates an interactively navigable scene from a single narrow-field-of-view image. This setting is challenging because the input observes only a small fraction of the environment, while interactive roaming requires a complete surrounding world, persistent geometry, controllable camera motion, and temporally coherent high-fidelity observations. MoVerse addresses this problem by separating world construction from observation rendering. It first expands the input into a gravity-aligned 360$^\circ$ panorama with topology-aware diffusion, closing the missing field of view before 3D reasoning. It then lifts the panorama into a persistent 3D Gaussian scaffold using panoramic geometry-aware residual prediction, yielding a dense and directly renderable spatial memory. Finally, a Gaussian-conditioned video renderer translates scaffold renderings along user-specified camera trajectories into photorealistic video. To make this renderer practical for interaction, we train a bidirectional diffusion teacher for high-quality conditional rendering and distill it into a causal autoregressive student for bounded-latency streaming. This design combines the controllability and long-range consistency of explicit 3D representations with the perceptual quality of generative video models. MoVerse supports real-time scene roaming at 8~FPS on a single NVIDIA RTX~4090 GPU, demonstrating a practical path toward single-image world creation with interactive video output.

---

## 论文详细总结（自动生成）

好的，基于您提供的论文内容，以下是对该论文的详细中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题：** 如何从**单张窄视场图像**出发，实时生成一个**可交互漫游**、具有**持久几何结构**、**相机可控**且**时间一致性高保真**的3D视频世界。
*   **研究动机与背景：** 此类应用（如VR原型、数字孪生、具身智能体模拟）的需求与单张图像信息严重不足的矛盾构成了根本挑战。现有方法主要分为两类，各有优劣：
    *   **显式3D方法：** 构建点云、网格、3D高斯等持久场景资产，提供稳定的空间记忆和精确的相机控制，渲染速度快。但直接从单张图像进行3D重建时，大量未观测区域需依赖弱证据推断，易产生空洞、浮游物、深度错误和有限感知质量。
    *   **隐式视频/世界模型：** 生成观测序列，视觉效果好，但几何一致性依赖模型的隐式记忆，在长轨迹或重复访问时容易发生场景漂移、身份改变或不连续。
    *   **混合系统：** 尝试结合两者，但存在“几何锚点的强度”与“渲染器的可部署性”之间的张力：稀疏锚点空间指导不足，强生成渲染器又过于昂贵或不适于实时交互。
*   **整体目标：** 提出一种实用路线，分离**世界构建**（离线）与**观察渲染**（在线），利用全景高斯的稠密、可直接渲染特性作为“空间内存”，并通过蒸馏技术实现实时、高质量的渲染。

### 2. 论文提出的方法论：核心思想、关键技术细节

*   **核心思想：** **三阶段分解**（Pipeline），将问题解耦为三个依次进行的阶段，分别解决**视场补全**、**3D几何构建**和**高质量渲染**问题。

*   **阶段 I：全景生成 → 解决视场补全**
    *   **目标：** 将输入NFOV图像扩增为重力对齐的360° 等距柱状投影（ERP）全景图，为后续阶段提供完整的视觉上下文。
    *   **关键技术细节：**
        1.  **规范全景表示：** 消除输入图像的俯仰和翻滚模糊，使地平线与ERP赤道对齐，为扩散模型提供一致的结构先验。
        2.  **可微分自动调平：** 使用可微分网络预测密集2D对应场，再通过刚性变换瓶颈（可微软argmin求解器）寻求最优球面旋转，避免非刚性畸变。
        3.  **拓扑感知全景补全：** 通过**圆形潜在编码**（使水平卷积填充环绕方位轴）和**移位等变生成**（对输入进行水平循环移位后，强制输出也应循环移位），保持ERP的水平S1拓扑闭合性。
        4.  **Horizon360数据集：** 自建的经过规范化的全景数据集，用于训练。所有全景图都被旋转到地平线与赤道对齐、垂直结构垂直的标准姿态。

*   **阶段 II：高斯场景生成 → 解决3D几何构建**
    *   **目标：** 将阶段I输出的规范全景图提升为**持久、可直接渲染**的3D高斯支架，作为下游渲染的几何与外观锚点。
    *   **关键技术细节：**
        1.  **全景高斯初始化：** 利用全景图的深度估计，通过**球面反投影**为每个像素初始化一个3D高斯。尺度初始化为与`D * cos(纬度)`成正比，考虑了高纬度区域的像素稀疏性。
        2.  **深度引导的残差预测：** 从全景图中提取多尺度特征，使用轻量级预测头为每个高斯预测其在**角度-逆深度空间**中的残差量（Δθ, Δφ, Δz等）。通过零初始化，网络从物理有意义的初始状态开始学习局部修正。
        3.  **属性组合：** 将预测的残差与初始化的基值组合，得到最终的高斯参数。中心位置在角度-逆深度空间中进行更新，保证了与ERP采样网格的一致性。
        4.  **多目标训练：** 训练损失包括目标视角的渲染损失（L1, LPIPS, SSIM）、源视角的重建损失、逆深度损失以及约束残差大小的正则化项。

*   **阶段 III：高斯条件视频渲染 → 解决高质量、实时渲染**
    *   **目标：** 在用户指定轨迹上，将阶段II的GS支架渲染结果转化为高质量、时间连续的视频，同时保持相机的可控性和场景布局。
    *   **核心技术细节：** 采用“教师-学生”蒸馏范式。
        1.  **双向扩散教师：** 基于预训练的Wan2.1-T2V，输入为“噪声目标视频潜变量”与“GS渲染视频潜变量”的拼接。使用**共享旋转位置编码**，让条件帧与目标帧在Transformer中处于相同位置，强制模型将条件视为密集对齐的渲染。训练一个高质量的双向条件扩散模型。
        2.  **因果自回归学生：** 将教师模型蒸馏为**因果自回归模型**，用于流式推理。学生模型逐块生成，仅依赖当前的GS渲染块和已生成的历史（通过局部KV缓存）。蒸馏过程采用Self-Forcing和DMD / RAVEN方法，保证学生模型在因果关系下的稳定性。
        3.  **训练数据混合：** 构建三种递进式训练对，模拟部署时的信号：从**轨迹对齐的几何代理**（点云重投影）→ 到**显式高斯渲染**（GS支架渲染）→ 再到**全景条件高斯渲染**（最终MoVerse接口）。这种课程式学习先教会模型修复通用伪影，再特化到GS伪影，最后适应MoVerse的特定接口。
        4.  **实时推理优化：** 分布式学生模型使用两步去噪、MemRoPE风格的局部KV缓存（1个起始帧、1个EMA记忆Token、3个局部帧的滑动窗口）和高效的TAEHV解码器，实现在RTX 4090上8 FPS的实时性能。

### 3. 实验设计：数据集、Benchmark与对比方法

*   **数据集：**
    *   **阶段I (全景生成)：** 使用了自建的**Horizon360**数据集（规范化的室内外全景图）和从其他数据源（如Matterport3D, Polyhaven HDRIs）采样的全景图。
    *   **阶段II (高斯场景生成)：** 使用了**HM3D**数据集进行训练。
    *   **阶段III (视频渲染)：** 使用了三种混合数据源：
        1.  **真实视频 + 点云重投影**：用于训练基本修复能力。
        2.  **真实视频 + GS渲染**：用于训练修复GS特有伪影。
        3.  **UE渲染全景 / Matterport3D / Polyhaven**：用于向全景-高斯接口适配。此外，还间接利用了LayerPano3D数据。
*   **Benchmark & 对比方法：**
    *   本文**没有**提供一个标准化的定量基准来与现有工作在相同指标上对比。实验以**定性结果展示**为主。
    *   **对比分析**在文本中进行，讨论了与显式3D方法（如WonderJourney, WonderWorld）、隐式世界模型（如Genie 3）以及其他混合系统（如EvoWorld, Gen3C）的设计差异和优劣，但**未提供定量对比表格**。

### 4. 资源与算力

*   **推理算力：** 明确说明在**单个NVIDIA RTX 4090 GPU**上，全套pipeline（特别是阶段III）支持**8 FPS**的实时交互。
*   **训练算力：** **论文中未明确说明**训练各个阶段所需的GPU数量、型号和时长。

### 5. 实验数量与充分性

*   **实验数量：** 论文结果章节主要提供了**定性展示**（多个图），包括：
    *   **全pipeline结果**（图6）：展示从单张图到最终视频的场景。
    *   **阶段I全景生成**（图7）：展示不同视角的输入和生成的全景图。
    *   **阶段II GS支架渲染**（图8）：展示直接从GS支架渲染的序列。
    *   **阶段III自回归渲染**（图9）：展示最终视频输出。
    *   **教师模型结果**（图10）：展示双向扩散教师模型的输出。
    *   **图1-5** 为方法示意图和过程展示。
*   **充分性与公平性分析：**
    *   **优点：** 定性结果视觉上令人信服，覆盖了全景图、3D渲染和最终视频等多个方面，直观展示了方法效果。
    *   **不足：**
        1.  **缺乏定量指标：** 这是最显著的不足。没有使用任何通用视频或图像质量指标（如PSNR, SSIM, LPIPS, FVD, FID）来量化与SOTA方法的差距。这使得方法的客观优势不明确。
        2.  **缺乏定量消融实验：** 没有对三个阶段的各个关键组件（如拓扑感知、残差预测、蒸馏策略、训练数据混合等）进行量化消融，以证明每个组件的具体贡献。
        3.  **缺乏对比：** 没有与最近的同类方法（如EvoWorld, Gen3C, WonderJourney等）进行定量或同场景下的定性对比。
        4.  **缺乏用户研究：** 没有报告关于交互体验、视觉质量或临场感等的用户研究。

### 6. 论文的主要结论与发现

*   **分离世界构建与观察渲染**是一个有效的系统设计策略，能够解耦不同任务（补全、3D重建、高清渲染）的挑战。
*   **稠密的全景高斯支架**作为持久空间记忆，为实时、长程稳定的交互提供了良好的几何锚点，优于稀疏点云或纯隐式模型。
*   **双向扩散教师蒸馏至因果自回归学生**的方案，成功地将高质量生成模型的能力与实时流式推理的需求结合起来，使得在有限算力下（RTX 4090）实现8 FPS的交互成为可能。
*   MoVerse证明了“单图到交互视频”的实用路径是可行的，即在显式控制和生成质量之间找到正确的平衡点。

### 7. 优点

*   **方法论的创新性：** 提出的三阶段分解是系统级的创新，清晰地划分了“世界构建”与“渲染”，解决了混合系统中最棘手的耦合问题。
*   **模块化与可扩展性：** 每个阶段都可以独立改进，系统接口清晰，方便后续研究与迭代。
*   **实时性能：** 在单张消费级显卡上达到8 FPS，这是迈向实际应用的关键一步，显著优于很多需要多GPU或无法实时运行的生成模型。
*   **处理了实际工程难题：** 针对全景图的拓扑闭合性、重力对齐、3DGS的初始化及残差预测、自回归模型的训练与推理等具体问题，提出了实用的解决方案。
*   **训练数据的课程设计：** 为阶段III设计的从几何代理到GS渲染再到全景GS的分级训练数据混合策略是巧妙且合理的，有助于模型更好地适应实际部署信号。

### 8. 不足与局限

*   **性能受限于stage I：** 最终的世界质量完全受限于全景补全的质量。如果补全出不一致或错误的场景，后续阶段无法修正，只会“更逼真地渲染错误”。
*   **3DGS的固有局限：** 高斯的近似本质导致在深度模糊、薄结构、反射表面、透明物体及ERP极地等区域仍存在浮游物、空洞或不稳定的深度累积问题。
*   **自回归渲染器的局部性：** 虽然GS支架保证了全局一致性，但自回归视频渲染器仍可能引入短期纹理漂移、过度平滑或对严重退化的条件输入的修正延迟。
*   **实验验证不足：** 如前所述，**最核心的缺陷**在于定量评估的缺失。没有提供与SOTA方法对比的量化指标和消融实验，使得方法的客观贡献和性能增益缺乏说服力。
*   **工作量与数据依赖：** 需要多个不同来源和类型的数据集进行训练，数据收集、处理、标注的工作量巨大，简化训练流程是未来的一个重要方向。模型训练的总体算力成本也未披露。
*   **静态场景假设：** 当前系统假设世界是静态的，未来需要扩展到可交互的动态物体或可编辑的世界。

（完）
