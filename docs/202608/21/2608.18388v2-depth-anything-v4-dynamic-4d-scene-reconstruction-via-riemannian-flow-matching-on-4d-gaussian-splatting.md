---
title: "Depth Anything V4: Dynamic 4D Scene Reconstruction via Riemannian Flow Matching on 4D Gaussian Splatting"
title_zh: Depth Anything V4：基于4D高斯泼溅的黎曼流匹配动态4D场景重建
authors: "Jiaming Fan, Jian Lu, Jinling Jia, Chenbin Zhang"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.18388v2"
tags: ["query:pano-dgs"]
score: 8.0
evidence: 使用4D高斯泼溅和黎曼流匹配从单目视频进行动态场景重建
tldr: 从单目视频重建动态4D场景，需在非欧几里得参数流形上优化高斯溅射，传统方法难以保证中间状态有效。本文提出DAV4，将黎曼流匹配（RFM）引入4D高斯溅射，在尺度、旋转、不透明度等流形上直接定义概率路径，确保生成过程始终合法。可控实验分离RFM与测试时优化及预训练的贡献：固定其余设置，MLP基线F-score为0.762，RFM达0.806，净增0.044；预训练成本360 GPU小时，可摊销至超万场景。DAV4在动态重建与新视图合成上超过先前Depth Anything系列及逐场景4D-GS，且不依赖人工深度标签。
source: arxiv
selection_source: fresh_fetch
motivation: 现有动态4D重建在高斯参数优化上忽视非欧几里得流形结构，中间状态常失效，且难以从单目视频高效学习。
method: 提出DAV4，用黎曼流匹配在4D高斯溅射的尺度、旋转、不透明度流形上定义概率路径，并配合测试时优化，无需人工深度监督。
result: RFM相比同设置MLP基线F-score从0.762提至0.806；预训练360 GPU小时，可扩展至超万场景；不确定性经NLL与ECE量化。
conclusion: DAV4超越先前Depth Anything及逐场景4D-GS，实现高质量动态4D重建与新视图合成，同时免除人工深度标签。
---

## 摘要
我们提出了Depth Anything V4 (DAV4)，一个从单目视频进行动态4D场景重建的框架。我们的关键贡献是将黎曼流匹配（RFM）应用于4D高斯泼溅参数，直接在非欧几里得流形（尺度、旋转、不透明度）上定义概率路径，确保所有中间状态都是有效的。通过受控实验，我们将RFM的贡献与测试时优化（TTO）和预训练分离。具有相同数据、架构和TTO的确定性MLP基线达到F分数0.762；RFM达到0.806——+0.044的提升是RFM的独立贡献。我们提供了修正后的计算成本分析：预训练为360 GPU小时，对于大规模部署（超过10,000个场景）可摊销。不确定性通过负高斯对数似然和期望校准误差进行量化。DAV4在动态重建和新视角合成方面优于先前的Depth Anything模型和逐场景4D-GS，同时不使用人工标注的深度标签作为训练损失。

## Abstract
We present Depth Anything V4 (DAV4), a framework for dynamic 4D scene reconstruction from monocular video. Our key contribution is the application of Riemannian Flow Matching (RFM) to 4D Gaussian Splatting parameters, defining probability paths directly on non-Euclidean manifolds (scale, rotation, opacity), ensuring all intermediate states are valid. Through controlled experiments, we isolate RFM's contribution from test-time optimization (TTO) and pre-training. A deterministic MLP baseline with the same data, architecture, and TTO achieves F-score 0.762; RFM achieves 0.806 - the +0.044 gain is RFM's isolated contribution. We provide corrected computational cost analysis: pre-training is 360 GPU-hours, amortizing for large-scale deployment (over 10,000 scenes). Uncertainty is quantified via Negative Gaussian Log-Likelihood and Expected Calibration Error. DAV4 outperforms prior Depth Anything models and per-scene 4D-GS on dynamic reconstruction and novel-view synthesis, while using no human-annotated depth labels as training losses.

---

## 论文详细总结（自动生成）

# 论文总结：《Depth Anything V4：基于4D高斯泼溅的黎曼流匹配动态4D场景重建》

> **说明**：论文原始PDF页面显示为撤回状态（arXiv ID 2608.18388v2 已不可用），以下总结基于论文元数据与摘要中的信息撰写。

## 1. 核心问题与研究动机

- **研究背景**：动态4D场景重建是计算机视觉与图形学的核心问题，目标是从单目视频中恢复随时间变化的3D场景结构，并支持新视角合成。
- **核心问题**：现有基于4D高斯泼溅（4D Gaussian Splatting）的动态重建方法，在优化高斯参数时忽视了参数空间的几何结构。高斯溅射的尺度（scale）、旋转（rotation）、不透明度（opacity）等参数定义在非欧几里得流形上，传统优化方法在流形间插值时会产生**无效的中间状态**，导致重建质量下降。
- **研究动机**：需要一种能够**尊重参数流形几何结构**的生成/优化方法，使4D高斯的演化过程始终保持合法，同时避免依赖人工标注的深度标签——因为深度标签的获取成本高昂。

## 2. 方法论：黎曼流匹配（RFM）用于4D高斯溅射

- **核心思想**：将黎曼流匹配（Riemannian Flow Matching, RFM）引入4D高斯泼溅的参数优化中。与传统的确定性MLP回归不同，RFM直接在尺度、旋转、不透明度所在的非欧几里得流形上定义概率路径，确保生成过程所有中间状态均处于合法参数空间内。
- **关键技术细节**：
  - 在**尺度流形**（正实数域）、**旋转流形**（单位四元数）和**不透明度流形**（有界区间）上分别构造概率路径；
  - 通过流匹配目标训练模型，学习从噪声分布到目标高斯参数分布的确定性传输；
  - **测试时优化（Test-Time Optimization, TTO）** 与预训练配合，在推理阶段进一步微调以适应特定场景。
- **无需人工深度监督**：整个训练流程不使用人工标注的深度图作为损失函数，规避了对昂贵深度标签的依赖。

## 3. 实验设计

- **数据集与场景**：元数据与摘要中未明确列出具体数据集名称（如DTU、NeRF-DS、D-NeRF等），仅提到"动态重建"和"新视角合成"的评测场景。
- **Benchmark**：采用动态重建质量（F-score）和新视角合成质量作为主评测指标；不确定性通过**负高斯对数似然（Negative Gaussian Log-Likelihood, NLL）** 和**期望校准误差（Expected Calibration Error, ECE）** 量化。
- **对比方法**：
  - 先前Depth Anything系列模型；
  - 逐场景优化的4D高斯泼溅（per-scene 4D-GS）；
  - 受控实验中对比的**确定性MLP基线**。
- **受控消融设计（核心亮点）** ：为分离RFM的独立贡献，作者确保MLP基线与RFM使用**相同的数据、架构和TTO设置**，唯一差异是是否采用RFM。

## 4. 资源与算力

- **预训练成本**：**360 GPU小时**。
- **说明**：文中有明确的总算力消耗，但**未说明GPU型号与数量**（如A100/H100及具体卡数），也未给出TTO阶段的单场景平均耗时。

## 5. 实验数量与充分性评估

- **实验组数**：摘要中明确报告的实验包括——
  1. 受控对比：MLP基线（F=0.762）vs. RFM（F=0.806），净提升+0.044；
  2. 与先前Depth Anything模型及per-scene 4D-GS的对比；
  3. 不确定性评估（NLL与ECE）。
- **充分性分析**：
  - **优点**：受控消融设计严谨，能清晰分离RFM贡献，避免"什么都加一点"的混淆；
  - **不足**：公开的元数据中未列出多个数据集的详细对比表、不同场景类型的分类评测、超参数敏感性分析等；由于PDF正文不可用，无法确认是否包含更全面的消融（如RFM中流形选择的逐一验证）。总体而言，实验设计方向正确，但**公开可见的证据数量偏少**，完整性受限。

## 6. 主要结论

- RFM在相同设置下比MLP基线F-score**提升0.044**，证明流形感知的概率路径定义是有效的；
- DAV4在动态4D重建与新视角合成上**优于先前Depth Anything系列和逐场景4D-GS**；
- 预训练成本360 GPU小时可在大规模部署（超10,000个场景）中摊销，具备实际应用潜力；
- 全程无需人工深度标签，降低数据依赖。

## 7. 优点

- **理论动机扎实**：明确指出高斯参数的非欧几里得结构，并用RFM在流形上直接建模，而非在欧氏空间近似；
- **实验控制严谨**：将RFM贡献从TTO和预训练中分离，结论可信度高；
- **不确定性量化**：通过NLL和ECE提供模型置信度评估，超越单纯指标对比；
- **实用性考虑**：360 GPU小时的预训练成本在大规模部署下可摊销，具备工程可行性。

## 8. 不足与局限

- **信息可获取性差**：论文PDF已被撤回，仅凭元数据难以验证完整技术细节与全部实验；
- **数据集信息缺失**：未指明具体评估数据集，难以判断泛化范围与场景多样性；
- **算力细节不足**：未说明GPU型号与数量，无法精确评估硬件门槛；
- **对比范围有限**：仅对比Depth Anything系列和4D-GS，未提及与其他动态重建方法（如MonST3R、Dynamic 3DGS变体等）的比较；
- **应用限制**：单目视频输入的固有歧义（遮挡、尺度漂移）问题未在摘要中讨论；超10,000场景的摊销推断是理论外推，尚未验证实际部署效果。

**（完）**
